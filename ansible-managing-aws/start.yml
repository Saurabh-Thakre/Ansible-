---
- hosts: localhost
  gather_facts: yes
  connection: local 
  vars:
    region: ap-southeast-2
    name: redhat
  tasks:
    - name: start the instance
      ec2:
        region: "{{ region }}"
        instance_ids: "i-01b2556bcc6bb849e"
        state: running
        wait: yes

